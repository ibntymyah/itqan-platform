# اتصال قاعدة البيانات
DATABASE_URL="postgresql://user:password@localhost:5432/itqan?schema=public"

# سرّ توقيع الجلسات — يجب أن يكون قيمة عشوائية طويلة، ويُدار كسرّ إنتاجي حقيقي
# (secrets manager) قبل الإنتاج، وليس ملف .env عادي — القسم 23
AUTH_SECRET="غيّر-هذه-القيمة-إلى-سلسلة-عشوائية-طويلة"

NODE_ENV="development"
